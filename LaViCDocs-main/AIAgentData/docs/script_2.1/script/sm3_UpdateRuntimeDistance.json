//获取实体的运行距离，并设置到自定义参数中
Agent.variables["runningdistance"].set(Agent.instance.runtime_d);
//运行距离超过航程
if(Agent.instance.runtime_d > Agent.variables["voyagedistance"].get())
{
    //设置实体的生命周期状态为停止位置迭代
    Agent._stop();
    //触发实体清理资源动作
    Agent.action_do("LifeSpanInvalidStopStepClearRes",1000);
}
